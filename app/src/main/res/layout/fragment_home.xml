<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:padding="16dp">

    <Button
        android:id="@+id/scan_btc_btn"
        style="@style/Widget.CryptoScanner.Button.Icon"
        android:layout_width="0dp"
        android:layout_height="@dimen/button_height"
        android:layout_marginEnd="8dp"
        android:layout_marginBottom="16dp"
        android:backgroundTint="@color/bitcoin_background"
        android:text="@string/bitcoin_label"
        android:textColor="@color/bitcoin_foreground"
        app:icon="@drawable/ic_bitcoin"
        app:iconTint="@color/bitcoin_foreground"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/scan_eth_btn"
        app:layout_constraintStart_toStartOf="parent" />

    <Button
        android:id="@+id/scan_eth_btn"
        style="@style/Widget.CryptoScanner.Button.Icon"
        android:layout_width="0dp"
        android:layout_height="@dimen/button_height"
        android:layout_marginStart="8dp"
        android:layout_marginBottom="16dp"
        android:backgroundTint="@color/ethereum_background"
        android:text="@string/ethereum_label"
        android:textColor="@color/ethereum_foreground"
        app:icon="@drawable/ic_ethereum"
        app:iconTint="@color/ethereum_foreground"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/scan_btc_btn" />

</androidx.constraintlayout.widget.ConstraintLayout>